# 明文激活码 —— 只发给用户，绝不提交到仓库
codes.txt

# 依赖 / 临时文件
node_modules/
*.log

# index.html 改动前的本地备份，绝不提交
index.html.bak-*
*.bak-*
*_backup.*

# Cloudflare wrangler 本地缓存（含登录 token 与账号邮箱，绝不提交）
.wrangler/

# 学校层级名单（含作者学校，仅存 KV，绝不提交到仓库）
backend/school-tiers.json

# 发货工具的后端管理密钥 + 发货流水（本地，绝不提交）
tools/.admin-secret
tools/issued-codes.txt
发货工具.bat
码池管理.html

.dev.vars*
!.dev.vars.example
.env*
!.env.example
